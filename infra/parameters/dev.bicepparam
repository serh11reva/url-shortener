using '../main.bicep'

// Optional overrides — main.bicep already defaults to westeurope, dev, rg-shortener-dev, single replica, cost-optimized SKUs.
// param apiContainerImage = '<your-registry>.azurecr.io/shortener-api:latest'
